provider "digitalocean" {
  # Token should be set via DIGITALOCEAN_TOKEN environment var.
  # Spaces credentials should be set with SPACES_ACCESS_KEY_ID and SPACES_SECRET_ACCESS_KEY environment variables.
}
